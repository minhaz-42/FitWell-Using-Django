# Local environment variables for NutriAI
# Copy real values into these fields (do NOT commit secrets to git)

# Local Qwen server settings
# Set the local model path and port used by the repo-local server
QWEN_LOCAL_MODEL_PATH=./models/Qwen
QWEN_LOCAL_PORT=21002
QWEN_DEVICE=cpu

# HTTP timeout (seconds) for requests to the local Qwen server
# Increase this if your model takes longer to generate responses.
QWEN_LOCAL_TIMEOUT=180

# Other example envs can go here
